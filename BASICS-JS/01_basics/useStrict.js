"use strict";//because it differentiate b/w the new java script and old js. means treat this file js code as new jscode.
//now its autoconfigured in many ides.
//alert(3 + 3); // it is used in the browers to print and seee the result in the pop up style but not here.
//because in browser the js in hidden inside it.
let a = 8 + 5;
console.log(a);

console.log(a);

//semicolons ; states the readabiltiy of the code improves.
///datatypes. 
/*
int , bigInt, boolean, string, null(standard value) let x = null , undefined .(let x;)
symbol -> for unique ness. this all comes under premitive datatypes. 
interview trick qustion is typeof null is obj
and typeof undefined is undefiend only.
now comes objects.

*/

console.log(typeof a);
console.log(typeof(a)); //both are same way of finding the type of the variable. 

//data conversion 
let x = 33;
 console.log(x);
 console.log(typeof x); // number right. but what if x = " 33 " then. i want to convert it into the 
 

